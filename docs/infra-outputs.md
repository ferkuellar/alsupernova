# Infra Outputs (Terraform)

Store the current outputs here for quick reference.

- S3 Web Bucket: (see `terraform output web_bucket_name`)
- CloudFront Domain: (see `terraform output cloudfront_domain`)

> Note: values may change if infra is recreated.
>
